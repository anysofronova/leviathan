{
  "name": "naur",
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "develop": "nx develop @naur/frontend",
    "develop:backend": "nx develop @naur/backend",
    "format": "nx run-many --target=format",
    "lint": "nx run-many --target=lint",
    "test": "nx run-many --target=test",
    "build": "nx build @naur/frontend",
    "serve": "nx serve @naur/frontend",
    "clean": "nx run-many --target=clean",
    "commit": "commit"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.4.2",
    "@commitlint/config-conventional": "^17.4.2",
    "husky": "^8.0.3",
    "lerna": "^6.4.1",
    "nx": "^15.6.3"
  },
  "engines": {
    "node": ">=18",
    "yarn": "1.22.19"
  },
  "packageManager": "yarn@1.22.19",
  "workspaces": [
    "packages/*"
  ]
}
